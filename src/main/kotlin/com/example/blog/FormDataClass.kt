package com.example.blog

//kotlin automatically defines getters and setters, for a data class this is all you need to use the variables in its
//primary constructor
data class MyForm(var tal:Int, var amount:Int)